package constants

// Role const
const (
	BUYER  = "buyer"
	SELLER = "seller"
)

// Endpoints
const (
	LoginEndpoint = "/api/v1/login"
)

// Header
const (
	HeaderContentType   = "Content-Type"
	HeaderAccept        = "Accept"
	MIMEApplicationJson = "application/json"
)
